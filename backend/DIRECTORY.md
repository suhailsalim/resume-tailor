# Directory

```
backend/src/
├── config/
│   └── config.ts       // Configuration settings (API keys, ports etc.)
├── controllers/
│   └── resumeController.ts // Request handling logic
├── middlewares/
│   └── uploadMiddleware.ts // Multer configuration
├── routes/
│   └── resumeRoutes.ts    // Defines API endpoints
├── services/
│   └── resumeService.ts   // Business logic (Langchain, file processing)
├── app.ts             // Main Express app setup
└── index.ts           // Optional: Entry point to start the server
```
